{
  "routes": [
    {
      "method": "POST",
      "path": "/user/register",
      "handler": "user.register",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/user/login",
      "handler": "user.login",
      "config": {
        "policies": []
      }
    }
  ]
}
